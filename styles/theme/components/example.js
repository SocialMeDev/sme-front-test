const example = {
  parts: [],
  baseStyle: ({ colorMode }) => ({
    bg: colorMode === 'dark' ? 'green.300' : 'green.500',
  }),
  sizes: {},
  variants: {},
  defaultProps: {},
}

export default example
